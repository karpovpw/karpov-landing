# Next.js static export SPA fallback - only for non-existing files
/*    /index.html   200

# Ensure Next.js static files take precedence
/_next/static/*    /_next/static/:splat  200
/api/*    /404.html   404

# Force HTTPS and enable HSTS
# https://karpov.pw/* https://karpov.pw/:splat 301!
# http://karpov.pw/* https://karpov.pw/:splat 301!

# Legacy redirects (update as needed)
# /old-path    /new-path    301

# Force HTTPS and non-www
# https://www.karpov.pw/* https://karpov.pw/:splat 301
# http://karpov.pw/* https://karpov.pw/:splat 301